.row
  .col-lg-12
    = title "Tags"

    %table.table
      %thead
        %th Tag
        %th Raised
        %th Charges

      - @tags.each do |tag|
        %tr
          %td= link_to(tag.name, organization_tag_path(current_organization, tag.name))
          %td= tag.total_raised
          %td= tag.total_charges_count
