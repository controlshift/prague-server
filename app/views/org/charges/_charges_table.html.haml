%table.table
  %thead
    %th Amount
    %th Currency
    %th Email
    %th Tags

  - charges.each do |charge|
    %tr
      %td= charge.amount
      %td= charge.currency
      %td= charge.customer.email
      %td= charge.tags.collect{ |t| link_to(t.name, org_tag_path(t.name)) }.join(', ').html_safe
