.row
  .col-lg-12
    = title @namespace.namespace

    %table.table
      %thead
        %th Tag
        %th Raised
        %th Charges

      - @namespaces.tags.each do |tag|
        %tr
          %td= tag.name
          %td= tag.total_raised
          %td= tag.total_charges_count