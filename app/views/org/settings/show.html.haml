.row
  .col-lg-12
    = breadcrumbs [[current_organization.name, organization_path(current_organization)],
                   ['Settings']]

    %h3 Default Widget Settings
    = render partial: 'organizations/global_defaults_form', locals: { valid: false}
    %h3 CRM Information
    = render partial: 'organizations/crm_form', locals: { valid: false}