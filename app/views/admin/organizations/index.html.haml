.row
  .col-lg-12
    = breadcrumbs [['Admin', admin_path],
                   ['Organizations']]

    = title 'Organizations'

    %table.table
      %thead
        %th Organization
        %th Charges

      - @organizations.each do |organization|
        %tr
          %td= link_to organization.name, admin_organization_path(organization)
          %td= organization.charges.size
